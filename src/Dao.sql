SELECT * FROM NOTICE


INSERT INTO NOTICE(
	CODE,
	TITLE,
	[CONTENT],
	MEMBER_ID)
VALUES(
	'2',
	'T1',
	'TEST TT',
	'ad')

SELECT * FROM 
(
	SELECT
		ROW_NUMBER() OVER (ORDER BY REGDATE DESC) NUM,
		NOTICE.* 
		FROM NOTICE WHERE TITLE LIKE '%%'
)N
WHERE NUM BETWEEN 1 AND 10